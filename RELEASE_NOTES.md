# Richmack: The Forest v2.2.2

- Forces title artwork through a real image element instead of a cached CSS background.
- Resolves MP3 paths against the server root and explicitly loads each track.
- Requests music playback before initializing the engine vibration audio.
- Adds cache-busting version parameters.
